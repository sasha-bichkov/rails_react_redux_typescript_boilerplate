version: '2'
options:
  verbose: true

syncs:
  rails-react-redux-typescipt-boilerplate-sync: # tip: add -sync and you keep consistent names als a convention
    src: './'
    sync_excludes: ['.git', '.idea', 'node_modules']
